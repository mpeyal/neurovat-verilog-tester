numpy
matplotlib
dearpygui>=1.11
# complex-script (Bangla/Devanagari) chat: shapes text with HarfBuzz + FreeType
# and draws it as an image (ImGui can't shape these). The chat falls back to
# plain text if these are absent.
uharfbuzz
freetype-py
# agent chat via an API key WITHOUT the Claude Code CLI (chat-only, no file/shell
# tools). Bundled into the .exe so an ANTHROPIC_API_KEY alone enables the agent.
anthropic
# optional - OpenAI provider for the agent chat (/provider openai):
# openai
